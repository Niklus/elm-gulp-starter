{
  "name": "elm-learning",
  "version": "1.0.0",
  "description": "learning elm",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "gulp",
    "build": "gulp compile && gulp copy && gulp compress && gulp bundle-sw",
    "elm-reactor": "node_modules/.bin/elm-reactor",
    "elm-make": "node_modules/.bin/elm-make src/app.elm --output www/app.js",
    "elm-packageInstall": "node_modules/.bin/elm-package install"
  },
  "dependencies": {
    "elm": "^0.18.0"
  },
  "devDependencies": {
    "browser-sync": "^2.18.13",
    "gulp": "^3.9.1",
    "gulp-uglify": "^3.0.0",
    "pump": "^1.0.2",
    "workbox-build": "^2.1.0"
  }
}
